import { ReferenceTypePipe } from './reference-type.pipe';

describe('ReferenceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReferenceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
