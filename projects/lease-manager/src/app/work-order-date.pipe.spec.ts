import { WorkOrderDatePipe } from './work-order-date.pipe';

describe('WorkOrderDatePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkOrderDatePipe();
    expect(pipe).toBeTruthy();
  });
});
