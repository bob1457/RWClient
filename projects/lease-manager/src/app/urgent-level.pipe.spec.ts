import { UrgentLevelPipe } from './urgent-level.pipe';

describe('UrgentLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new UrgentLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
