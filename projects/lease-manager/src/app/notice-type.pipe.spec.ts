import { NoticeTypePipe } from './notice-type.pipe';

describe('NoticeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NoticeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
