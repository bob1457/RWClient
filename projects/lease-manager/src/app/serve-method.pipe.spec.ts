import { ServeMethodPipe } from './serve-method.pipe';

describe('ServeMethodPipe', () => {
  it('create an instance', () => {
    const pipe = new ServeMethodPipe();
    expect(pipe).toBeTruthy();
  });
});
