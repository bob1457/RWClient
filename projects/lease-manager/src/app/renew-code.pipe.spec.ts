import { RenewCodePipe } from './renew-code.pipe';

describe('RenewCodePipe', () => {
  it('create an instance', () => {
    const pipe = new RenewCodePipe();
    expect(pipe).toBeTruthy();
  });
});
